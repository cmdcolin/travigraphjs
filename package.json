{
  "name": "travigraphjs",
  "version": "1.0.0",
  "description": "plot travis-ci build times on the web",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "vega": "^3.0.0-beta.28",
    "vega-lite": "^1.3.1"
  },
  "scripts": {
    "build": "cp index.html dist; browserify index.js > dist/index.js",
    "build:watch": "watch 'npm run build' ."
  },
  "devDependencies": {
    "watch": "^1.0.2"
  }
}
